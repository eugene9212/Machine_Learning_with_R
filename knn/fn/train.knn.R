
# Train code for KNN #

train.knn <- function(train.x, train.y){
  
  # Memorize all training data
  # Actually nothing to do with train code
  
  # return
  obj <- list(train.x = train.x, train.y = train.y)
}